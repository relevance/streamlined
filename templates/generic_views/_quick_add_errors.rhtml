<div class="error">
	<big>Error updating <%= @quick_add_model_name %>:</big><br/>
	<%= @quick_add_model.errors.full_messages.join('<br/>') %>
	<br/>
	<% @quick_add_model.class.delegate_targets.each do |dt| %>
		<% assoc = @quick_add_model.class.reflect_on_association(dt) %>
		<% assoc_name = assoc.class_name.underscore.to_sym %>
		<%= instance_variable_get("@#{assoc_name}").errors.full_messages.join('<br/>') %>
    <% end %>
</div>